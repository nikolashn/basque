#{ 
	Dumb multiplication algorithm as a basic demonstration of closures
	Expected output: 139797
#}

u64 DumbMul(u64 a, u64 b) {
	u64 SmartAdd(u64 x, u64 y) {
		return x + y;
	}
	u64 product = 0;
	while b; --b {
		product += a;
	}
	return product;
}

f"%u{DumbMul(951, 147)}\n";

